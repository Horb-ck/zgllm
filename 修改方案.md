# 注册功能修改方案

## 目标

在 `student` 表中允许预先写入“只有学号”的占位行。注册时：

- 如果填写的学号已存在于 `student` 表，且该行尚未绑定邮箱/密码，则视为预置学生，允许使用任意格式合法的邮箱注册。
- 如果填写的学号不存在于 `student` 表，则仍然只允许使用 `@stu.cqu.edu.cn` 结尾的学生邮箱注册。
- 教师注册规则暂不放宽，继续要求 `@cqu.edu.cn` 邮箱，避免影响现有身份规则。
- 前端不再对所有学生强制 `@stu.cqu.edu.cn`，最终准入规则以后端为准。

## 当前代码观察

- 后端注册入口在 `app.py` 的 `/register`，当前会查询：
  `SELECT sid, email FROM student WHERE sid = %s OR email = %s`
- 现有逻辑只要发现 `sid` 已存在，就直接提示“账号已被注册”。这会导致预置的“只有学号”行无法被认领注册。
- 注册前还需要先调用 `/auth/send_code` 发送验证码。如果这个接口不做同样的邮箱策略校验，用户可能先收到验证码，最后提交注册时才被拒绝，体验会比较割裂。
- `templates/auth/register.html` 里有三处前端强校验：
  - `emailPatterns.student` 固定要求 `@stu.cqu.edu.cn`
  - `updateFields()` 给邮箱输入框设置了学生邮箱 `pattern`
  - 表单提交和“发送验证码”按钮都会用 `emailPatterns[role]` 拦截
- 当前注册插入 SQL 有一个明显问题：`INSERT INTO student (sid, email, password, role, sis_id)` 有 5 个字段，但 `VALUES (%s, %s, %s, %s)` 只有 4 个占位符。改注册逻辑时需要一起修正，否则新学号注册分支会失败。

## 后端方案

### 1. 抽出统一的注册邮箱策略判断

在 `app.py` 中增加一个小的内部辅助函数，例如：

```python
def _is_student_email(email):
    return str(email or '').strip().lower().endswith('@stu.cqu.edu.cn')

def _is_teacher_email(email):
    return str(email or '').strip().lower().endswith('@cqu.edu.cn')

def _is_blank(value):
    return value is None or str(value).strip() == ''
```

再提供一个“查询学号注册状态”的内部函数，使用参数化 SQL 和 `with closing(get_conn())`：

```python
def _get_student_row_by_sid(cursor, sid, for_update=False):
    sql = "SELECT sid, email, password, role, sis_id FROM student WHERE sid = %s"
    if for_update:
        sql += " FOR UPDATE"
    cursor.execute(sql, (sid,))
    return cursor.fetchone()
```

这里 `for_update=True` 只在真正写入注册信息时使用，避免并发下两个请求同时认领同一个预置学号。

### 2. `/auth/send_code` 增加注册场景的准入校验

`send_verification_code()` 目前只检查邮箱和账号是否为空，然后直接发验证码。建议在 `scene == 'register'` 时增加校验：

- 从请求里读取 `role`，前端发送验证码时一并传入。
- 如果 `role == 'teacher'`：必须是 `@cqu.edu.cn`。
- 如果 `role == 'student'`：
  - 查询 `student.sid = account`
  - 如果存在未注册预置行，允许任意格式合法邮箱
  - 如果不存在预置行，必须是 `@stu.cqu.edu.cn`
  - 如果该学号已经有邮箱或密码，直接返回“账号已被注册”
- 如果邮箱已经被其他账号使用，返回“邮箱已被注册”

这样可以避免“不符合规则的邮箱也能收到验证码”的问题。

### 3. `/register` 改成“认领预置行或插入新行”

注册提交时的核心分支建议改为：

1. 基础校验：`role`、验证码、邮箱格式、密码确认（后端最好也补一次 `confirm_password` 校验）。
2. 开启 MySQL 连接，在事务中查询：
   - `SELECT sid, email, password, role, sis_id FROM student WHERE sid = %s FOR UPDATE`
   - `SELECT sid FROM student WHERE email = %s AND sid <> %s`
3. 判断邮箱是否被其他账号占用，若占用则拒绝。
4. 判断学号状态：
   - 学号存在，且 `email/password` 都为空：这是预置行。验证码通过后执行 `UPDATE student SET email=%s, password=%s, role=%s, sis_id=%s WHERE sid=%s`。
   - 学号存在，但 `email` 或 `password` 已有值：这是已注册账号，拒绝。
   - 学号不存在：如果学生邮箱不是 `@stu.cqu.edu.cn`，拒绝；验证码通过后执行 `INSERT`。
5. 验证码 `_verify_code("register", username, email, verification_code)` 必须放在最终写库前。

`INSERT` 分支需要修正为 5 个占位符：

```sql
INSERT INTO student (sid, email, password, role, sis_id)
VALUES (%s, %s, %s, %s, %s)
```

### 4. 密码存储说明

当前登录逻辑是明文比较 `result[0] == password`，本次需求不建议顺手改成哈希，否则需要同步改登录和存量用户迁移。实现时先保持现有行为，避免扩大变更范围；后续可以单独做密码哈希迁移。

## 前端方案

修改 `templates/auth/register.html`：

- 学生邮箱输入框不要再设置固定的 `@stu.cqu.edu.cn` HTML `pattern`。
- 表单提交时：
  - 教师仍校验 `@cqu.edu.cn`。
  - 学生只校验浏览器 `type="email"` 能识别的基础邮箱格式，不在前端强制域名。
  - 学生邮箱是否必须是 `@stu.cqu.edu.cn` 交给后端根据学号是否预置判断。
- “发送验证码”按钮同样取消学生域名强拦截，并把 `role` 一起提交给 `/auth/send_code`。
- 邮箱补全建议保留 `@stu.cqu.edu.cn` 作为学生默认建议，但不代表强制规则。
- 学生身份提示文案从“使用学号与校内邮箱”改成更准确的“预置学号可使用常用邮箱”或类似文案。

如果希望前端体验更明确，可以额外增加一个轻量接口，例如 `/auth/register_policy?sid=xxx&role=student`，在用户输入学号后返回：

```json
{"preloaded": true, "allow_any_email": true}
```

不过为了控制改动，第一版可以不新增接口，只让后端返回清晰错误信息。

## 推荐错误提示

- 预置学号已被注册：`该学号已完成注册，请直接登录`
- 非预置学号使用外部邮箱：`该学号未在预置名单中，请使用 @stu.cqu.edu.cn 邮箱注册`
- 邮箱重复：`邮箱已被注册`
- 教师邮箱不合规：`教师邮箱需使用 cqu.edu.cn 域名`

## 手动验证清单

- 预置行：`sid=20240001, email=NULL, password=NULL`，使用 QQ/Gmail 等普通邮箱注册，应能发送验证码并注册成功，最终更新同一行。
- 预置行已注册：同一 `sid` 再次注册，应提示账号已注册。
- 非预置学号 + `@stu.cqu.edu.cn`，应能发送验证码并注册成功，走 `INSERT` 分支。
- 非预置学号 + 外部邮箱，应在发送验证码或提交注册时被拒绝。
- 教师 + 非 `@cqu.edu.cn` 邮箱，应被拒绝。
- 邮箱已被其他账号使用，应被拒绝。

## 需要确认的问题

1. 预置的“只有学号”行里，`email` 和 `password` 是 `NULL`，还是空字符串？方案会同时兼容两者，但数据库唯一索引下更推荐使用 `NULL`。
我再添加时会确保是NULL
2. 预置学号允许任意邮箱时，是否也允许教师身份使用任意邮箱？当前方案默认只对学生放宽，教师不放宽。
教师不管
3. 学号存在但只有邮箱、没有密码，或者只有密码、没有邮箱时，要按“已注册”处理，还是也视为可认领的异常预置行？
理论上不会出现这种情况，请直接视为可认领的异常预置行